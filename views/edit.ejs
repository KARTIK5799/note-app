<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        body {
            background: radial-gradient(circle at 50% 50%, #1a1a2e, #16213e);
        }

        .glassmorphism {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .container {
            display: flex;
            justify-content: center;
            gap: 2rem;
        }

        .form-container {
            width: 100%;
            max-width: 600px;
        }

        .form-container form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
    </style>
</head>

<body class="bg-gray-900 text-white font-sans leading-relaxed">
    <a href="/" class="inline-block mt-4 ml-5 text-blue-400 hover:text-blue-300 transition-colors font-semibold">
        <span class="material-symbols-outlined">arrow_back</span> Back to Task List
    </a>

    <div class="main flex flex-col items-center w-full h-screen py-10 overflow-hidden">
        <div class="container">
            <!-- Read-Only Form -->
            <div class="form-container glassmorphism p-8 rounded-lg shadow-2xl">
                <h2 class="text-2xl font-bold mb-4 text-blue-300">Current Task Content</h2>
                <form>
                    <div>
                        <label for="currentTitle" class="block text-lg font-semibold mb-2">Title:</label>
                        <input type="text" id="currentTitle" value="<%= filename %>" readonly
                            class="block w-full px-5 py-3 glassmorphism text-white bg-gray-800 placeholder-gray-400 outline-none shadow-md rounded-lg">
                    </div>
                    <div>
                        <label for="currentDescription" class="block text-lg font-semibold mb-2">Description:</label>
                        <textarea id="currentDescription" readonly
                            class="block w-full px-5 py-3 glassmorphism text-white bg-gray-800 placeholder-gray-400 outline-none shadow-md rounded-lg" rows="10"><%= filedata %></textarea>
                    </div>
                </form>
            </div>

            <!-- Update Form -->
            <div class="form-container glassmorphism p-8 rounded-lg shadow-2xl">
                <h2 class="text-2xl font-bold mb-4 text-blue-300">Update Task Content</h2>
                <form action="/update/<%= filename %>" method="post">
                    <div>
                        <label for="newTitle" class="block text-lg font-semibold mb-2">Title:</label>
                        <input type="text" id="newTitle" name="title" value="<%= filename %>" required
                            class="block w-full px-5 py-3 glassmorphism text-white placeholder-gray-400 outline-none shadow-md rounded-lg">
                    </div>
                    <div>
                        <label for="newDescription" class="block text-lg font-semibold mb-2">Description:</label>
                        <textarea id="newDescription" name="description" required
                            class="block w-full px-5 py-3 glassmorphism text-white placeholder-gray-400 outline-none shadow-md rounded-lg" rows="10"><%= filedata %></textarea>
                    </div>
                    <input type="submit" value="Update Task"
                        class="w-full bg-blue-600 hover:bg-blue-700 transition-all p-3 rounded-lg text-white font-bold cursor-pointer shadow-lg btn-glow">
                </form>
            </div>
        </div>
    </div>
</body>

</html>
